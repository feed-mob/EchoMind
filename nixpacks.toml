providers = ["node"]

[phases.setup]
nixPkgs = ["curl", "unzip"]
cmds = ["curl -fsSL https://bun.sh/install | bash -s -- bun-v1.3.8"]

[phases.install]
cmds = ["cd api && /root/.bun/bin/bun install --frozen-lockfile"]

[start]
cmd = "cd api && /root/.bun/bin/bun run start"
